// ---------------------------------------------------------------
// Fill this in with YOUR Firebase project's config to turn on
// cloud sync between devices. Until you do, Aurora still works
// perfectly as a local, on-this-device notes app.
//
// How to get these values (free, ~5 minutes):
//   1. Go to https://console.firebase.google.com and create a project.
//   2. In the project, click "Build > Firestore Database" > Create
//      database > Start in test mode (fine for a private two-person app).
//   3. Click the gear icon > Project settings > scroll to "Your apps" >
//      click the </> (web) icon > register the app.
//   4. Copy the firebaseConfig object it gives you into the object below.
//
// Full walkthrough is in README.md.
// ---------------------------------------------------------------

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
